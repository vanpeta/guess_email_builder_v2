calendar.datepicker();
function Email (rows) {
	this.brand : function () {
		checkboxes.on('click', function () {
				for (var i = 0; i < checkboxes.length; i++) {
					checkboxes[i].checked = false;
				}
				this.checked = 'checked';
			return var brand = this.value;
			});
		},
	this.date : calendar.datepicker('getDate'),
	this.rows: []
};


new Email (brand, date, rows);
